{
    "compilerOptions": {
        // Ini memberitahu VS Code untuk mengasumsikan kode menargetkan versi modern (ES2020)
        // yang mendukung fitur seperti Optional Chaining (?. ) dan arrow functions asyn/await.
        "target": "ES2020", 
        "module": "commonjs"
    },
    // Pastikan folder ini diabaikan untuk kecepatan
    "exclude": [
        "node_modules",
        "**/node_modules"
    ]
}